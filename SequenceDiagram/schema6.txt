@startuml
skinparam sequence {
	ArrowColor #186336
}

footer Diagramma 6 su 11
participant	BlockClient
participant	"BlockServer(Thread GestioneClient)"


BlockClient -> "BlockServer(Thread GestioneClient)": Invia N
activate "BlockServer(Thread GestioneClient)" #69bbe2
activate BlockClient #69bbe2

alt N <= SIZE

	loop N times
"BlockServer(Thread GestioneClient)"-> "getBlocco (Lista.h)": Richiedi Blocco con ID e genesi
activate "getBlocco (Lista.h)" #69bbe2
"getBlocco (Lista.h)" --> "BlockServer(Thread GestioneClient)": Ritorna il Blocco 
deactivate "getBlocco (Lista.h)" 
		"BlockServer(Thread GestioneClient)" -> BlockClient: Invio i-esimo Blocco
	end
else N > SIZE
	
	"BlockServer(Thread GestioneClient)" -> BlockClient: Invio N = -1
end

@enduml
